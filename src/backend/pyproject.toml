[project]
name = "denied-api"
version = "0.1.0"
description = "Local judgment API for denied."
requires-python = ">=3.11"
dependencies = [
    "python-dotenv>=1.0,<2",
    "fastapi>=0.115,<1",
    "httpx>=0.28,<1",
    "pydantic>=2.10,<3",
    "uvicorn>=0.34,<1",
    "psycopg[binary]>=3.2,<4",
]
