{
  "name": "denied",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun build.ts",
    "typecheck": "tsc --noEmit",
    "test:api": "cd backend && uv run --env-file .env python -m unittest discover -s tests -v",
    "test:extension": "bun tests/extension.smoke.ts",
    "test:video": "bun tests/video.browser.ts && bun tests/video.real.ts",
    "test:adapters": "bun tests/adapters.browser.ts && bun tests/adapters.real.ts",
    "observe:adapters": "bun tests/adapters.live.ts",
    "test:telemetry": "uv run --project backend --env-file backend/.env bun tests/telemetry.smoke.ts",
    "test:live": "bun tests/live.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.3.0",
    "@types/chrome": "^0.1.0",
    "playwright": "^1.55.0",
    "typescript": "^5.9.0"
  }
}
