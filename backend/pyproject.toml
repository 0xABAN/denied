[project]
name = "denied-backend"
version = "0.1.0"
description = "Private decision telemetry API for denied."
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.115,<1",
  "psycopg[binary]>=3.2,<4",
  "uvicorn[standard]>=0.30,<1",
]

[tool.uv]
package = false
